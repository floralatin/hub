on: [push]

jobs:
  console:
    runs-on: ubuntu-latest
    steps: 
      - run: echo  The tags are ${{ secrets.ACCESS_TOKEN }} 
    

  # hub-actions:
  #   uses: floralatin/hub-actions/.github/workflows/hub-actions.yml@master
  #   with:
  #     action: 'all'
  #   secrets: 
  #     access_token: ${{ secrets.ACCESS_TOKEN }}